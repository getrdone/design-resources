# Color library conventions

- Top-level materials gallery folder is named `# materials` (sorts first).
- When adding any new theme (download import or generated), always place `{theme}--materials-mood--01.png` in `# materials/` and keep `# materials/{theme}/MATERIALS.txt`.
